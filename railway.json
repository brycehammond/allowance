{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet publish src/AllowanceTracker/AllowanceTracker.csproj -c Release -o out"
  },
  "deploy": {
    "startCommand": "cd out && dotnet AllowanceTracker.dll",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
