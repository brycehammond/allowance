# ===========================================
# Fastlane Environment Variables
# ===========================================
# Copy this to .env and fill in your values
# NEVER commit .env to git!
#
# Quick setup:
# 1. cp .env.default .env
# 2. Fill in the values below
# 3. Run: bundle exec fastlane validate
# ===========================================

# ===========================================
# App Store Connect API Key (REQUIRED)
# ===========================================
# Generate at: https://appstoreconnect.apple.com/access/api
#
# Steps:
# 1. Go to App Store Connect -> Users and Access -> Keys
# 2. Click "+" to generate a new key with Admin role
# 3. Download the .p8 file and save as AuthKey.p8 in fastlane/
# 4. Note the Key ID and Issuer ID
#
APP_STORE_CONNECT_API_KEY_ID=
APP_STORE_CONNECT_API_ISSUER_ID=
APP_STORE_CONNECT_API_KEY_PATH=./fastlane/AuthKey.p8

# ===========================================
# Team IDs (Optional - usually auto-detected)
# ===========================================
# Only needed if you belong to multiple teams
#
# Developer Portal Team ID
# Find at: https://developer.apple.com/account/#/membership
# TEAM_ID=

# App Store Connect Team ID
# Find at: App Store Connect -> Users and Access -> Your account
# ITC_TEAM_ID=
